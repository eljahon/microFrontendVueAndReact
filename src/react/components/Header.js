import React, {useState} from 'react';
const Header = () => {
    const name = "Eljahon react component"
    return (
        <div>
           <h1>{name}</h1>
        </div>
    );
}

export default Header;
